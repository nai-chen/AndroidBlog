<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Context.getFilesDir() -->
    <files-path name="files_image" path="." />

    <!-- Context.getCacheDir() -->
    <cache-path name="cache_image" path="." />

    <!-- Context.getExternalFilesDir(null) -->
    <external-files-path name="external_files_image" path="." />

    <!-- Context.getExternalCacheDir() -->
    <external-cache-path name="external_cache_image" path="." />

    <!-- Environment.getExternalStorageDirectory() -->
    <external-path name="external_image" path="." />

</paths>