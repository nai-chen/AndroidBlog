package com.blog.demo;

import junit.framework.TestCase;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by cn on 2017/3/22.
 */

public class ToolTestCase extends TestCase {

    public void testJoin() throws Exception {
        List<String> favList = new ArrayList<String>();
        String deli = ",";

//        assertEquals("", Tool.join(favList, deli));
//
//        favList.add("电影");
//        assertEquals("电影", Tool.join(favList, deli));
//
//        favList.add("旅游");
//        assertEquals("电影,旅游", Tool.join(favList, deli));
//
//        favList.add("唱歌");
//        assertEquals("电影,旅游,唱歌", Tool.join(favList, deli));
    }
}
